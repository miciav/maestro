dag:
  name: "test_all_skipped"
  fail_fast: false
  tasks:
    - task_id: start
      type: PythonTask
      condition: "False"

    - task_id: success1
      type: PythonTask
      condition: "False"
      dependencies: ["start"]

    - task_id: failer1
      type: PythonTask
      condition: "False"
      dependencies: ["start"]

    - task_id: final
      type: PythonTask
      condition: "False"
      dependencies: ["failer1"]
