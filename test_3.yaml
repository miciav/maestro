dag:
  name: "test_exit_skipped"
  tasks:
    - task_id: start
      type: BashTask
      params:
        command: "exit 1"

    - task_id: cleanup
      type: PrintTask
      params:
        message: "Cleanup"
      dependencies: ["start"]
      dependency_policy: "none"
      is_final: true
